version: 2
# No constraint: the `sections` list is an ordered grammar (§3.2), so repeats
# of one rule may sit together but not straddle the next rule's occurrences.
title: null
sections:
  - match: "A"
    repeat: "0..n"
  - match: "B"
    repeat: "0..n"
extras: anywhere
