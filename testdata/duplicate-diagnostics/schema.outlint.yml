version: 2
title: null
sections:
  # Two rules with the same matcher label in one scope: when neither is
  # satisfied, both report the same missing header.
  - id: first-goal
    match: "/Goal/"
    required: true
  - id: second-goal
    match: "/Goal/"
    required: true
forbid_sections:
  # Two headers with the same text and depth share one path, so guarding them
  # produces two byte-identical entries.
  - match: "Notes"
extras: anywhere
