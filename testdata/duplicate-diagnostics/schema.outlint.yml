version: 1
title: null
sections:
  # Two headers with the same text and depth share one path, so denying them
  # produces two byte-identical entries.
  - id: banned
    match: "Notes"
    allow: false
  # Two rules with the same matcher label in one scope: when neither is
  # satisfied, both report the same missing header.
  - id: first-goal
    match: "/Goal/"
    required: true
  - id: second-goal
    match: "/Goal/"
    required: true
