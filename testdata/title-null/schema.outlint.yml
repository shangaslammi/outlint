version: 2
# `title: null` declares a document with no `h1` at all: the `sections` scope
# binds the document's own top-level `h2`s, and a present `h1` is forbidden
# wholesale at the title node, with its subtree validated no further.
title: null
sections:
  - match: "Overview"
    required: true
extras: anywhere
