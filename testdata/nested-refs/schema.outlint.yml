version: 2
title: null
sections:
  - id: group
    match: "Group"
    required: false
    sections:
      - id: child
        match: "Child"
        required: false
    extras: anywhere
    constraints:
      - requires: { if: child, then: "$.other" }
  - id: other
    match: "Other"
    required: false
extras: anywhere
constraints:
  - any_of: [group.child, other]
